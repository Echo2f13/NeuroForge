# NeuroForge — Dependencies
# All tools are free/open-source. No paid services required.

# === LLM Framework ===
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langgraph>=0.2.0

# === LLM Providers (all free-tier) ===
groq>=0.9.0
openai>=1.40.0                # Used for OpenRouter and GitHub Models (OpenAI-compatible API)

# === Embeddings (local, free) ===
sentence-transformers>=3.0.0

# === Vector Database (local, free) ===
chromadb>=0.5.0

# === Knowledge Graph (in-memory, free) ===
networkx>=3.3

# === Document Processing ===
pdfplumber>=0.11.0
PyMuPDF>=1.24.0               # fitz — fallback PDF reader
python-pptx>=1.0.0
python-docx>=1.1.0

# === OCR (local, free) ===
paddleocr>=2.7.0
paddlepaddle>=2.6.0           # PaddleOCR backend
pytesseract>=0.3.10           # Tesseract fallback OCR
Pillow>=10.0.0                # Image processing

# === YouTube Transcripts (free, no API key) ===
youtube-transcript-api>=0.6.0

# === Data Models & Validation ===
pydantic>=2.7.0

# === Environment & Configuration ===
python-dotenv>=1.0.0

# === Web Framework (API) ===
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9       # File uploads

# === Jupyter ===
jupyter>=1.0.0
ipykernel>=6.29.0

# === Visualization (for mind maps & graphs) ===
matplotlib>=3.9.0

# === Utilities ===
tiktoken>=0.7.0               # Token counting
tqdm>=4.66.0                  # Progress bars

# === Testing ===
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0                 # For FastAPI TestClient
